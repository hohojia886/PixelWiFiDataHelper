<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="io.github.hohojia886.pixelwifidatahelper.PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="io.github.hohojia886.pixelwifidatahelper.PERMISSION" />

    <application
        android:allowBackup="true"
        android:directBootAware="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.PixelWiFiDataHelper">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Pixel WiFi Data Helper" />
        <meta-data
            android:name="xposedminversion"
            android:value="102" />

        <activity
            android:name=".SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".utils.RemotePrefProvider"
            android:authorities="io.github.hohojia886.pixelwifidatahelper.prefs"
            android:exported="true"
            android:directBootAware="true" />

    </application>

</manifest>
